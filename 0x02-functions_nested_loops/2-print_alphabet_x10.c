#include <stdio.h>
#include <main.h>
/**
 * main - check the code
 *
 * Return: always return to 0
 *
 */
int main(void)


{	
	for (char j = 1; j <= 10; j++)
	{ for (char i = 'a'; i <= 'z'; i++)
		{ putchar(i);
		}
		putchar('\n');
	}
	return (0);
}
